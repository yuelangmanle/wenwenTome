# 文文Tome 开发书

> **版本**：v1.0.0 | **最后更新**：2026-03-04 | **维护者**：Antigravity AI + Owner

---

## 一、项目核心定位与目标

**“本地优先的全平台、全能电子书与网文追更解决方案。”**

**核心壁垒与解决痛点**：
- 解决 Calibre 移动端体验差、同步麻烦的问题。
- 解决微信读书等商业 App 强制联网、广告多、版权限制的问题。
- 解决 KOReader 功能强但 UI 丑、新手门槛高的问题。
- **终极特性**：PC端管理 + 移动端阅读、局域网一键同步、全格式与网文支持、沉浸式极简现代化 UI、完全本地化零强制联网、完全掌控书库。
- **账户与服务器**：完全无后台服务器，零账户体系。设备之间通过设备码（或扫码）点对点局域网同步。

---

## 二、架构设计与技术栈

### 2.1 技术选型
- **全栈框架**：Flutter 3.x（Dart），单一代码库横跨 Windows 和 Android。
- **状态管理**：flutter_riverpod（响应式声明式状态管理）。
- **路由管理**：go_router（基于 URL 声明式导航）。
- **持久化方案**：原生 File/Directory 操作 + JSON 序列化（轻量无外部依赖，弃用 ISAR），配合 path_provider。

### 2.2 核心模块架构树
```text
lib/
├── app/               # 路由表 (router.dart)、全局现代化主题 (theme.dart)
├── features/
│   ├── library/       # 【书库主阵地】监控文件夹，提取元数据，格式分析 (Book模型)
│   ├── reader/        # 【沉浸阅读器】EPUB (epub_view) / PDF (pdfx) 渲染，字号/行距/背景色持久化联动
│   ├── annotations/   # 【核心外围工具】高亮划线、笔记批注、书签引擎
│   ├── search/        # 【全文检索】基于 compute isolate 的后台倒排与正则匹配
│   ├── sync/          # 【同步心脏】mDNS 服务发现，双端 HTTP REST Server/Client 实现互通
│   ├── webnovel/      # 【网文引擎】规则解析器，多书源抓取与离线洗白
│   ├── metadata/      # 【元数据增强】豆瓣 API / Open Library 在线补全抓取
│   ├── pkm/           # 【知识联动】导出批注至 Obsidian 等本地 Vault (Markdown 格式)
│   ├── translation/   # 【离线/在线辅助】LibreTranslate 本地引擎 + MyMemory 在线机器翻译
│   └── settings/      # 【全局中枢】同步路径、语言、深浅模式等基础配置
└── main.dart          # 入口点，依赖组装
```

---

## 三、已实现功能矩阵 (v1.0.0 MVP)

- **多格式支持**：原生解析 EPUB, PDF, MOBI, AZW3, TXT, CBZ/CBR。
- **精美书架**：现代极简设计，8 种格式专属渐变色封面卡片，底部格式徽章蒙版。
- **沉浸阅读**：深色/浅色自适应，护眼模式；阅读器顶部/底部栏 FadeTransition 动效隐藏。
- **本地极客级管理**：JSON 持久化，自动保存进度（300字/翻页触发），局域网 mDNS 一键扫码双向同步。
- **知识网联**：5 色高亮划词，云端/本地元数据搜刮，全书/当前页中英翻译，以及一键导出 Obsidian Markdown 笔记。
- **网文引擎**：打破单机电子书限制，支持自定义聚合书源，边下边读。

---

## 四、版本迭代规则（必须严格遵守）

> 📌 每次迭代开发完成后，必须按以下顺序执行，一步不能少。

### 4.1 版本号规则
- 格式：`主.功能.修复`（如 `1.0.0`）
- **功能迭代**：第二位 +0.1（`1.0.0 → 1.1.0 → 1.2.0`）
- **Bug 修复**：第三位 +0.0.1（`1.0.0 → 1.0.1`）
- **PC 端与 Android 端版本号始终同步**，不允许出现不一致

### 4.2 每次迭代必做清单

```
1. 更新 version.json         — 版本号、日期、两端平台版本
2. 更新 CHANGELOG.md         — 在顶部追加新版本的功能/修复内容
3. 更新 开发书.md             — 更新功能清单、架构变更
4. 更新 开发进度书.md         — 记录本次迭代内容和状态
5. flutter build windows     — 构建 Windows 版本
6. flutter build apk --release — 构建 Android APK
7. 将 CHANGELOG.md 复制到:
   - releases/vX.Y.Z/CHANGELOG.md（归档内附更新日志）
8. 归档 Windows 版:
   - 默认模式：将 `build/windows/x64/runner/Release` 的内容复制到 `releases/vX.Y.Z/windows/`（**直接用文件夹，提供免安装便携式体验，禁止打包为压缩包或安装包**）。
   - 特例模式：**仅当用户特别要求时**，才需要使用 Inno Setup 打包制作带安装地址选择的真正 `.exe` 安装程序。
9. 归档 Android APK:
   - `releases/vX.Y.Z/wenwen_tome_vX.Y.Z.apk`
10. Git commit + 创建 Tag `vX.Y.Z`
```

### 4.3 归档目录结构

```
releases/
├── v1.0.0/
│   ├── windows/                          ← PC 便携版文件夹（直接双击 .exe）
│   │   ├── wenwen_tome.exe
│   │   ├── flutter_windows.dll
│   │   ├── data/
│   │   └── ...
│   ├── wenwen_tome_v1.0.0.apk            ← Android 安装包
│   ├── CHANGELOG.md                      ← 此版本更新日志
│   └── release_notes.md                  ← 发行说明
├── v1.1.0/
│   └── ...
└── README.md
```

---

## 五、开发环境配置

| 工具 | 版本/位置 |
|------|----------|
| Flutter SDK | 系统 PATH（`flutter doctor` 确认） |
| Android SDK | `C:\Users\猫甜\AppData\Local\Android\Sdk` |
| Android NDK | SDK 内 `ndk/28.2.13676358`（2.1GB） |
| Gradle 缓存 | `E:\Dev\gradle-home`（已迁 E 盘） |
| AGP 版本 | 8.7.3 |
| Kotlin 版本 | 2.0.21 |
| Gradle 版本 | 8.9 |

### 构建命令
```powershell
# Android APK（需设置 E 盘 Gradle）
$env:GRADLE_USER_HOME="E:\Dev\gradle-home"
flutter build apk --release

# Windows 便携版
flutter build windows
```

---

## 六、已知问题 / 技术债

| 优先级 | 问题 | 备注 |
|--------|------|------|
| 中 | EPUB 字号/行距设置仅在 ReaderSettings 面板中调整，epub_view 渲染层未直接联动 | epub_view 3.x API 限制 |
| 低 | widget_test.dart 挂起超 27 小时 | 需要重写测试 |
| 低 | flutter analyze 仍有 ~10 个 info 级提示 | 不影响运行 |
