# 文文Tome 开发书

> **版本**：v1.0.0 | **最后更新**：2026-03-04 | **维护者**：Antigravity AI + Owner

---

## 一、项目概述

**文文Tome** 是一款本地优先的跨平台电子书阅读器，支持 Windows 桌面端与 Android 手机端。  
核心理念：**数据本地、功能完整、设计精美、双端同步**。

### 支持格式
EPUB · PDF · MOBI · AZW3 · TXT · CBZ · CBR

### 技术栈
| 层级 | 技术 |
|------|------|
| 框架 | Flutter 3.x（Dart） |
| 状态管理 | flutter_riverpod |
| 路由 | go_router |
| 本地存储 | JSON + path_provider |
| EPUB 渲染 | epub_view |
| PDF 渲染 | pdfx |
| 网络同步 | shelf + HTTP REST |
| 设备发现 | multicast_dns |

---

## 二、架构设计

```
lib/
├── app/               # 路由、主题
├── features/
│   ├── library/       # 书架管理（Book 模型、LibraryService、Providers）
│   ├── reader/        # 阅读器（EPUB/PDF 渲染、ReaderSettings 持久化）
│   ├── annotations/   # 高亮笔记、书签
│   ├── search/        # 全文搜索
│   ├── sync/          # 局域网同步（SyncServer、SyncClient、mDNS）
│   ├── webnovel/      # 网文抓取器
│   ├── metadata/      # 在线元数据（豆瓣 / OpenLibrary）
│   ├── pkm/           # Obsidian 笔记导出
│   ├── translation/   # 全书翻译
│   └── settings/      # 设置界面
└── main.dart
```

---

## 三、已实现功能清单（v1.0.0）

- ✅ 多格式本地书库管理（导入、进度跟踪、JSON 持久化）
- ✅ EPUB 沉浸式阅读器（FadeTransition UI、字号/行距/背景色持久化）
- ✅ PDF 全屏渲染
- ✅ 5 色高亮划词笔记 + 书签系统
- ✅ 全文后台搜索（compute isolate）
- ✅ 局域网 QR 码同步（PC ↔ Android）
- ✅ 网文在线抓取（多书源）
- ✅ 在线元数据补全（豆瓣 + OpenLibrary）
- ✅ Obsidian PKM 笔记导出（Markdown + YAML frontmatter）
- ✅ 全书翻译（LibreTranslate 本地 + MyMemory 在线备选）
- ✅ 渐变格式封面卡片 + 精美空书架引导

---

## 四、版本迭代规则（必须严格遵守）

> 📌 每次迭代开发完成后，必须按以下顺序执行，一步不能少。

### 4.1 版本号规则
- 格式：`主.功能.修复`（如 `1.0.0`）
- **功能迭代**：第二位 +0.1（`1.0.0 → 1.1.0 → 1.2.0`）
- **Bug 修复**：第三位 +0.0.1（`1.0.0 → 1.0.1`）
- **PC 端与 Android 端版本号始终同步**，不允许出现不一致

### 4.2 每次迭代必做清单

```
1. 更新 version.json         — 版本号、日期、两端平台版本
2. 更新 CHANGELOG.md         — 在顶部追加新版本的功能/修复内容
3. 更新 开发书.md             — 更新功能清单、架构变更
4. 更新 开发进度书.md         — 记录本次迭代内容和状态
5. flutter build windows     — 构建 Windows 版本
6. flutter build apk --release — 构建 Android APK
7. 将 CHANGELOG.md 复制到:
   - releases/vX.Y.Z/CHANGELOG.md（归档内附更新日志）
8. 归档 Windows 版:
   - releases/vX.Y.Z/windows/  — 直接放文件夹（免安装便携式）
   - 注意: 不使用 ZIP，直接用文件夹
9. 归档 Android APK:
   - releases/vX.Y.Z/wenwen_tome_vX.Y.Z.apk
10. Git commit + 创建 Tag `vX.Y.Z`
```

### 4.3 归档目录结构

```
releases/
├── v1.0.0/
│   ├── windows/                          ← PC 便携版文件夹（直接双击 .exe）
│   │   ├── wenwen_tome.exe
│   │   ├── flutter_windows.dll
│   │   ├── data/
│   │   └── ...
│   ├── wenwen_tome_v1.0.0.apk            ← Android 安装包
│   ├── CHANGELOG.md                      ← 此版本更新日志
│   └── release_notes.md                  ← 发行说明
├── v1.1.0/
│   └── ...
└── README.md
```

---

## 五、开发环境配置

| 工具 | 版本/位置 |
|------|----------|
| Flutter SDK | 系统 PATH（`flutter doctor` 确认） |
| Android SDK | `C:\Users\猫甜\AppData\Local\Android\Sdk` |
| Android NDK | SDK 内 `ndk/28.2.13676358`（2.1GB） |
| Gradle 缓存 | `E:\Dev\gradle-home`（已迁 E 盘） |
| AGP 版本 | 8.7.3 |
| Kotlin 版本 | 2.0.21 |
| Gradle 版本 | 8.9 |

### 构建命令
```powershell
# Android APK（需设置 E 盘 Gradle）
$env:GRADLE_USER_HOME="E:\Dev\gradle-home"
flutter build apk --release

# Windows 便携版
flutter build windows
```

---

## 六、已知问题 / 技术债

| 优先级 | 问题 | 备注 |
|--------|------|------|
| 中 | EPUB 字号/行距设置仅在 ReaderSettings 面板中调整，epub_view 渲染层未直接联动 | epub_view 3.x API 限制 |
| 低 | widget_test.dart 挂起超 27 小时 | 需要重写测试 |
| 低 | flutter analyze 仍有 ~10 个 info 级提示 | 不影响运行 |
