# 文文Tome 开发进度书

> 记录每次迭代的具体内容、完成状态和问题。每次迭代后必须更新此文件。

---

## 当前版本：v1.0.0「初见」

**状态**：✅ 已发布  
**发布日期**：2026-03-04  
**构建状态**：Windows ✅ | Android APK ✅（57.3MB）

---

## 迭代历史

### v1.0.0 — 2026-03-04「初见」首发版

#### 完成内容

**阶段 1 — 核心骨架**
- [x] Flutter 跨平台项目初始化（Windows + Android）
- [x] MaterialApp.router + flutter_riverpod + go_router 架构
- [x] Material 3 深色/浅色主题（深蓝+金配色）

**阶段 2 — 书籍解析与基础阅读器**
- [x] Book 数据模型（支持 7 种格式）
- [x] LibraryService（JSON 持久化、导入、进度更新）
- [x] ReaderScreen（EPUB + PDF 渲染，沉浸模式 FadeTransition）
- [x] ReaderSettingsProvider（字号/行距/背景色/夜间模式，持久化）

**阶段 3 — 局域网同步**
- [x] SyncServer + SyncClient（HTTP REST）
- [x] mDNS 设备广播
- [x] QR 码扫码配对界面

**阶段 4 — 扩展阅读功能**
- [x] AnnotationService（5 色高亮、笔记、书签）
- [x] FullTextSearch（compute isolate 后台搜索）
- [x] 网文抓取器（WebNovelScraper + WebNovelScreen）

**阶段 5 — 进阶功能**
- [x] MetadataService（豆瓣 + OpenLibrary）
- [x] ObsidianSyncService（Markdown 导出）
- [x] TranslationService（LibreTranslate + MyMemory）
- [x] SettingsScreen

**UI 精美化**
- [x] 格式专属渐变封面卡片（8 种格式颜色）
- [x] 底部格式徽章蒙版
- [x] 精美空书架引导页
- [x] 阅读器底部进度条

**代码清理**
- [x] 移除 isar / isar_flutter_libs（Gradle 不兼容）
- [x] 移除 network_info_plus（Kotlin 编译失败）
- [x] Lint 问题从 36 个降至 10 个（全 info 级）

**构建 & 发布**
- [x] Android NDK 2.1GB 重新安装
- [x] AGP 8.7.3 + Kotlin 2.0.21 + Gradle 8.9 + kotlin.incremental=false
- [x] GRADLE_USER_HOME → E:\Dev\gradle-home（E 盘）
- [x] APK 57.3MB 构建成功
- [x] version.json + CHANGELOG.md + releases/v1.0.0/ 归档（含 APK）
- [x] Git Tag v1.0.0

#### 待解决

| 问题 | 优先级 |
|------|--------|
| EPUB 字号在 epub_view 中未直接联动（API 限制） | 中 |
| widget_test.dart 挂起，需重写 | 低 |
| Windows 归档需从 ZIP 改为文件夹形式 | 低 |

---

## 下次迭代计划（v1.1.0）

> 计划日期：待定

### 候选功能
- [ ] EPUB 字号真实联动（改用 flutter_html 渲染，替换 epub_view）
- [ ] EPUB 封面自动提取并缓存
- [ ] 阅读进度自动保存（300 字/翻页触发）
- [ ] Android NSD 扫描（同步更顺畅）
- [ ] Windows 归档改为直接文件夹形式（已在 releases/README 注明）
- [ ] 重写 widget_test.dart

---

## 迭代规则提醒

每次完成迭代后，必须依次执行：

```
1. 更新 version.json（版本号、日期）
2. 更新 CHANGELOG.md（顶部追加）
3. 更新 开发书.md（功能清单 / 架构）
4. 更新 开发进度书.md（本文件，记录本次迭代）
5. flutter build windows + flutter build apk --release
6. 拷贝 CHANGELOG.md → releases/vX.Y.Z/CHANGELOG.md
7. 拷贝 Windows 构建目录 → releases/vX.Y.Z/windows/（文件夹，非 ZIP）
8. 拷贝 APK → releases/vX.Y.Z/wenwen_tome_vX.Y.Z.apk
9. git commit + git tag vX.Y.Z
```
